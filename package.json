{
  "name": "replicate-line-bot.ts",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@biomejs/biome": "1.3.1",
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@line/bot-sdk": "^8.0.2",
    "hono": "^3.9.0",
    "replicate": "^0.22.0"
  },
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "dev:driver": "bun run src/driver.ts",
    "format": "biome format --write ./src",
    "predev": "bun run format",
    "serve": "bun run src/index.ts"
  }
}